using System;
using PicoContainer;
using System.IO;
using System.Data;

namespace NanoContainer.Script.Xml
{
	/// <summary>
	/// Summary description for XMLContainerBuilder.
	/// </summary>
	public class XMLContainerBuilder //: ScriptedContainerBuilder 
	{
    DataSet ds = new DataSet();

/*    public XMLContainerBuilder(StreamReader stream): base(stream) {
      ds.ReadXml(stream);
    }

    protected IMutablePicoContainer CreateContainerFromScript (IPicoContainer parentContainer, object assemblyScope)
    {

    }

    protected override void ComposeContainer (IMutablePicoContainer container, object assemblyScope) {
      // do nothing. assume that this is done in createContainer().
    }

    public void BuildContainer (out IPicoContainer containerRef, ref IPicoContainer parentContainerRef, object compositionScope)
    {
      
    }
*/
//    public abstract void KillContainer (IPicoContainer containerRef);
  }
}
