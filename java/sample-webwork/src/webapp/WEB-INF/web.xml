<!DOCTYPE web-app PUBLIC "-//Sun Microsystems, Inc.//DTD Web Application 2.3//EN" "http://java.sun.com/dtd/web-app_2_3.dtd" >

<web-app>
    <display-name>WebWork NanoContainer Demo</display-name>

    <context-param>
        <param-name>nanocontainer.groovy</param-name>
        <param-value><![CDATA[
            pico = new org.picocontainer.defaults.DefaultPicoContainer(parent)
            if(assemblyScope instanceof javax.servlet.ServletContext) {
                System.out.println("**** Webapp-level pico components instantiated by Groovy")
                pico.registerComponentImplementation(org.nanocontainer.sample.webwork.dao.simple.MemoryCheeseDao)
            } else if(assemblyScope instanceof javax.servlet.http.HttpSession) {
                System.out.println("**** Session-level pico components instantiated by Groovy")
            } else if(assemblyScope instanceof javax.servlet.ServletRequest) {
                System.out.println("**** Request-level pico components instantiated by Groovy")
            }
            return pico
        ]]></param-value>
    </context-param>

    <listener>
        <listener-class>org.nanocontainer.webwork.WebWorkContainerListener</listener-class>
    </listener>

    <servlet>
        <servlet-name>WebWork</servlet-name>
        <servlet-class>org.nanocontainer.webwork.PicoServletDispatcher</servlet-class>
        <load-on-startup>1</load-on-startup>
    </servlet>

    <servlet-mapping>
        <servlet-name>WebWork</servlet-name>
        <url-pattern>*.action</url-pattern>
    </servlet-mapping>

</web-app>


